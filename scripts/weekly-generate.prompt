You are the AI Agent Intelligence knowledge base system administrator responsible for the GitHub Pages site at https://zfcheng.github.io/ai-agent-intelligence/.

Your task is to generate the WEEKLY summary for the current week and fully publish it.

IMPORTANT: Use the current system date to determine the ISO week. First, run a terminal command to confirm the current ISO week:
Run `date +%G-%V` (or equivalent Python code for ISO year-week) and use the output as <WEEK> (format YYYY-WW, e.g. 2026-25).

Then:
- Check if the file /Users/zfcs/kworkspace/AIDoing/info-search/docs/weekly/<WEEK>.md already exists (use `test -f` or `ls`).
- If it already exists, output "Weekly summary for <WEEK> already exists. Nothing to do." and stop.
- If it does not exist, proceed to generate it (this handles cases where the Mac was off at the scheduled time).

1. Strictly follow and invoke the ai-agent-intelligence skill:
   - Adhere 100% to the topic scope (production reliability, harness/observability/eval like LangSmith, self-improving loops, LangGraph etc.).
   - Prioritize the target accounts in the skill references.
   - Use X search tools (x_keyword_search / x_semantic_search) and semantic/web search to gather the latest high-signal information for the past week. X is the PRIMARY source — prioritize it.
   - X-tool resilience (IMPORTANT): the X backend can return transient "Deadline" / "upstream" errors. If an X tool call fails, RETRY it up to 2 more times before giving up on that single query. A transient failure on one query must NOT stop the remaining X queries. Only fall back to web search for a theme after its X queries have been retried and still failed.
   - Output in the exact Weekly Summary template from the skill: more structured report with sections Trend Overview, Framework & Tool Updates (especially LangGraph, LangSmith), Self-Improving Loop Progress, Production & Reliability Insights, Key Discussions from Target Accounts, Action Items for User / Team (3-5 concrete suggestions).

2. The final generated content MUST start exactly with this YAML frontmatter (use the confirmed <WEEK>):

---
title: "AI Agent Weekly Summary - <WEEK>"
week: "<WEEK>"
type: weekly
tags: ["AI Agent", "Production", "Harness", "Self-Improving"]
---

Followed by the full professional Markdown body.

3. Persist the content:
   - Use the write tool (or equivalent) to create/overwrite the file at the absolute path:
     /Users/zfcs/kworkspace/AIDoing/info-search/docs/weekly/<WEEK>.md
   - Write the COMPLETE content including frontmatter.

4. Update the dashboard:
   - Read the current docs/index.md
   - In the "## Weekly" section, add at the very top (after the heading):
     - [<WEEK>](./weekly/<WEEK>.html) — <short one-sentence highlight from the summary, e.g. key trend or action item>
   - Save the updated index.md using the write tool.

5. Publish to GitHub:
   - Execute these exact terminal commands in order (use run_terminal_command):
     cd /Users/zfcs/kworkspace/AIDoing/info-search
     git add docs/weekly/<WEEK>.md docs/index.md
     git commit -m "chore: add AI Agent weekly summary for <WEEK>"
     git push
   - Confirm the push succeeded.

6. Finally, output a short confirmation:
   "Weekly summary for <WEEK> generated via ai-agent-intelligence skill, saved to docs/weekly/<WEEK>.md, index updated, and pushed to GitHub. Pages URL: https://zfcheng.github.io/ai-agent-intelligence/weekly/<WEEK>.html"

Do not skip any step. Always generate the actual summary by using the ai-agent-intelligence skill (do not make up content without research). Use tools for all file operations and git. Be precise with paths and frontmatter. Use ISO week numbering.